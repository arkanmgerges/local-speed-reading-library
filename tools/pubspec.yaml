name: lsr_library_tools
description: Import, normalise, validate, build and publish-check tooling for the Local Speed Reading public-domain library.
version: 0.1.0
publish_to: none
repository: https://github.com/arkanmgerges/local-speed-reading-library

environment:
  sdk: ^3.13.0

dependencies:
  archive: ^4.3.0
  args: ^2.7.0
  crypto: ^3.0.7
  html: ^0.15.7
  http: ^1.6.0
  json_schema: ^5.2.2
  path: ^1.9.1
  unorm_dart: ^0.3.2
  xml: ^7.0.1

dev_dependencies:
  lints: ^6.1.0
  test: ^1.32.0
